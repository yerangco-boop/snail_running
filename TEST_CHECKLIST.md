# 달팽이 러닝 — 실외 테스트 체크리스트

실기기(안드로이드)에 새 버전을 설치한 뒤 이 순서대로 점검한다.
결과는 `[ ]` → `[x]`로 바꾸고, 이상이 있으면 그 줄 아래에 증상을 적는다.

## 0. 테스트 전 준비 (v18부터 필수)

- [ ] **위치 권한이 "항상 허용"인지 확인** — 시작 화면 상단에 주황색 경고 배너가 보이면
      아직 아님. 배너의 "설정 열기" → 권한 → 위치 → **항상 허용** 선택
- [ ] **배터리 최적화 해제** — 설정 → 애플리케이션 → 달팽이 러닝 → 배터리 →
      **제한 없음**(또는 최적화 안 함). 이게 켜져 있으면 백그라운드에서 앱이 억제됨
- [ ] 러닝 시작 후 **알림창에 "달팽이 러닝 / 러닝 기록 중"** 알림이 뜨는지 확인
      (이 알림이 있어야 포그라운드 서비스가 실제로 붙은 것)
- [ ] 비교 기준으로 나이키런(또는 다른 러닝 앱) 동시 실행

## 1. 기능 전수 점검 (12항목)

| # | 항목 | 확인 방법 | 결과 |
|---|------|-----------|------|
| 1 | 설정 저장 | 설정 변경 → 앱 완전 종료 → 재실행 → 값이 유지되는지 | [ ] |
| 2 | TTS 음성 선택 | 설정 → TTS 음성 → 목록에서 미리듣기 후 선택 → 러닝 중 해당 음성으로 안내 | [ ] |
| 3 | 메트로놈 BPM | 설정에서 BPM 변경 → 러닝 중 실제 박자가 바뀌는지 | [ ] |
| 4 | 메트로놈 볼륨 | **이어폰 없이 스피커로** 최대 볼륨에서 야외 주행 중 들리는지 | [ ] |
| 5 | 5개 테마 전환 | 미드나잇 바이올렛/아크틱 블루/에메랄드 나이트/로즈 골드/앰버 프레스티지 전부 전환, 글자 안 깨지는지 | [ ] |
| 6 | 날씨 연동 | 시작 화면에 조회시각·지역명·기온/습도/강수확률이 뜨는지 | [ ] |
| 7 | 케이던스 | 러닝 중 중앙 숫자를 케이던스로 순환 → 나이키런과 비슷한 값(130~150spm)인지 | [ ] |
| 8 | 칼로리 | 빠른 페이스와 느린 페이스의 kcal/분이 **다르게** 나오는지 | [ ] |
| 9 | GPS 폴리라인 | 주행 중/이력 상세 지도에 경로선이 실제 이동 경로대로 그려지는지 | [ ] |
| 10 | km 마커 | 1km 지날 때마다 지도에 km 배지가 찍히는지 | [ ] |
| 11 | 랩 마커 | 바퀴 완료 시 지도에 깃발 마커 + 중앙 숫자 "바퀴"가 올라가는지 | [ ] |
| 12 | 히스토리 DB | 종료 후 이력 탭에 기록 저장 → 카드 탭 → 상세(지도+통계+랩) 표시 | [ ] |

## 2. v18 핵심 검증 (이번 버전에서 고친 것)

- [ ] **화면 꺼짐 테스트** — 러닝 중 화면을 끄고 5분 이상 뛴 뒤, 그 구간 거리가
      정상 누적됐는지 확인. (v17까지 이 구간이 통째로 유실돼 -7.9~-54.7% 오차 발생)
      - "러닝 중 화면 켜두기" 설정이 ON이면 화면이 안 꺼지는지도 함께 확인
- [ ] **거리 정확도** — 나이키런 대비 오차 몇 %인지 기록:
      - 달팽이: ____ km / 나이키: ____ km / 오차: ____ %
- [ ] **랩 카운트** — 실제 몇 바퀴 돌았는지 세면서 뛰고 앱 값과 비교:
      - 실제: ____ 바퀴 / 앱: ____ 바퀴
      - (v18에서 40m/18m/60초로 재산정 — 기존 80m는 코스가 작아 물리적으로 도달 불가였음)
- [ ] **랩 기준 거리 vs GPS 거리** — 이력 카드/상세에 병기되는 두 값 비교:
      - 랩 추정: ____ km / GPS: ____ km
- [ ] **케이던스** — 나이키런 값과 비교:
      - 달팽이: ____ spm / 나이키: ____ spm

## 3. 테스트 후 반드시 할 것

- [ ] **설정 → 디버그 → "실외 테스트 로그 공유"** 로 로그 파일 저장/전송
      - 로그에서 확인할 태그: `[GPS]` `[CADENCE]` `[LAP]` `[Perm]`
      - `[Perm]` 줄에 `always`가 찍혔는지 (whileInUse면 백그라운드 권한 미승인 상태)
      - `[CADENCE]` 10초 요약의 `수신이벤트` 수가 0으로 떨어지는 구간이 있는지
        (있으면 스트림이 죽은 것, 없는데 `임계값통과`가 0이면 임계값 문제)
      - `[LAP]` 줄의 `최대이격` 값 — 이 값이 코스 크기를 알려주므로 임계값 재조정 근거가 됨
- [ ] 위 표에서 실패한 항목을 CLAUDE.md "다음 작업 목록"에 옮겨 적기

## 참고: 로그 태그별 의미

| 태그 | 의미 |
|------|------|
| `[Perm]` | 위치 권한 상태 (always / whileInUse / denied) |
| `[GPS]` | 매 위치 갱신의 정확도·순간속도·누적거리·수락/거부 포인트 수 |
| `[CADENCE]` | 10초마다 수신 이벤트 수 / 임계값 통과 수 / 필터링된 mag 범위 |
| `[LAP]` | 아웃존 진입, 현재·최대 이격거리, 경과 시간, 카운트 판정 결과 |
| `[Metro]` | 메트로놈 초기화 상태 |
| `[Weather]` | 날씨 API 조회 결과 |
