# 🏃‍♂️ 슬로우 조깅 앱(달팽이 러닝) 명세서 v2

> 이 문서는 기존 SPEC.md(제미나이 작업 인수인계 버전)를 기반으로, 나이키 런 클럽 앱과 SJP(슬로우 조깅 메트로놈) 앱을 참고하여 업데이트한 버전입니다. Claude Code로 재작성 시 이 문서를 기준으로 진행합니다.

## 0. 기존 코드 상태 (참고)

- 기존 `lib/main.dart`는 114줄 수준의 매우 기본적인 단일 화면 코드
- 거리 추적(페이스 기반 가상 계산), 1초 타이머, BPM 선택(140~200), TTS 안내 정도만 구현
- 탭 구조, SQLite 저장, 메트로놈 클릭음, GPS, 지도, 스트레칭 연동 등은 **모두 미구현 상태**
- 따라서 기존 코드는 참고만 하고, 본 명세서 기준으로 대부분 새로 작성

## 1. 디자인 레퍼런스

- **러닝 중 화면 / 이력 화면**: 나이키 런 클럽(Nike Run Club) 스타일 참고
  - 화면 중앙에 큰 숫자로 핵심 정보 표시, 하단 원형 컨트롤 버튼
  - 이력 화면은 카드/리스트 형태로 과거 운동 기록 표시
- **세션 설정 화면**: SJP(슬로우 조깅 메트로놈) 앱 스타일 참고
  - 다크 네이비 + 보라색 포인트 컬러
  - 둥근 알약 모양 버튼, 카드형 옵션 선택 UI
- **전체 테마**: OLED Black / Deep Navy 다크모드 기본

## 2. 러닝 중 화면 (메인 화면) — 우선순위 핵심

화면에 표시되는 정보 (나이키 스타일, 거리가 메인):

| 정보 | 비중 |
|---|---|
| **거리 (km)** | 가장 크게, 화면 중앙 |
| **경과 시간** | 거리 아래 보조 정보 |
| **평균 페이스 (분/km)** | 거리 아래 보조 정보 |
| BPM/케이던스 | 작게, 보조 표시 (예: "♪ 150 BPM") |
| 지도 (선택적 표시) | GPS 경로를 화면 상단 또는 토글로 표시 |

하단에는 원형 일시정지/정지 버튼.

## 3. 메트로놈 & 오디오

- BPM 선택: 140~200, 10단위 (기존과 동일, 170 포함)
- 클릭음(메트로놈 사운드) 실제 재생 구현 — 기존 코드에는 빠져 있던 핵심 기능
- **음악 동시 재생 지원**: 유튜브/음악 앱이 재생 중일 때도 메트로놈 클릭음이 함께 들리도록 오디오 믹스 모드 설정 (Android: `AudioAttributes` ducking 비활성화 / iOS: `AVAudioSession` mixWithOthers 옵션)
- **설정에서 토글 가능**: "다른 오디오와 함께 재생" 옵션 (기본값: ON)

## 4. 음성 안내 (TTS)

| 시점 | 안내 내용 |
|---|---|
| 매 1km 구간 도달 | "1km 도달, 구간 시간 OO분 OO초, 평균 페이스 OO분 OO초" |
| 목표 거리의 50% 지점 | "목표의 절반을 지났습니다" 안내 |
| 목표 거리 도달 | 축하 음성 (기존 명세서 내용 유지) — **타이머/거리는 멈추지 않고 계속 진행** |
| 종료 시 | 최종 요약 음성 (선택적) |

- **음성 성별 선택 가능**: 설정 화면에서 남성/여성 TTS 음성 선택

## 5. 무중단 시스템 (기존 명세서 핵심 원칙 유지)

- 목표 거리 도달 시에도 타이머와 거리 카운팅은 절대 멈추지 않음
- 사용자가 직접 "정지" 버튼을 눌러야만 운동 종료
- 종료 시점에 최종 거리, 시간, 평균 페이스, GPS 경로를 기록으로 저장

## 6. GPS 위치 추적 & 지도 경로 표시 (신규)

- `geolocator` 패키지로 실시간 위치(위도/경도) 수집 (일정 간격, 예: 5초)
- **지도 표시는 OpenStreetMap 기반 (`flutter_map` 패키지) 사용 — 완전 무료, API 키 불필요**
  - Google Maps는 무료 사용량 제한 및 API 키 발급이 필요하므로 제외
- 수집된 좌표를 선으로 이어 달린 경로를 지도에 표시
- 종료 시 경로 데이터를 SQLite에 함께 저장하여, 이력 화면에서 과거 경로도 다시 볼 수 있도록 함
- ⚠️ GPS는 실내/PC 환경에서는 정상 테스트 불가 — 실제 휴대폰으로 야외에서 테스트 필요

## 7. 사후 케어 & 통계 (기존 명세서 유지)

- 로컬 DB 백업: SQLite(`snail_running_db.db`)에 날짜, 시간, 평균 페이스, 거리, GPS 경로, 미드풋 유지 여부 저장
- 통증 맞춤형 스트레칭 연동: 종료 다이얼로그에서 통증 부위 체크 → '김병곤 [선택부위] 스트레칭' 키워드로 유튜브 연동 (URL Launcher)

## 8. 이력 화면 (신규 — 나이키 스타일)

- 과거 운동 기록을 카드/리스트 형태로 표시
- 각 기록에 날짜, 거리, 시간, 평균 페이스 표시
- 탭하면 해당 운동의 GPS 경로 지도와 상세 정보 확인 가능

## 9. 화면/탭 구조

기존 명세서의 IndexedStack 기반 탭 구조 유지:
- **홈/현재 러닝 탭**: 시작 전 화면(SJP 스타일 오늘 개요) → 러닝 중 화면(나이키 스타일)
- **세션 설정**: BPM, 목표 거리/시간, 음성 성별, 오디오 믹스 토글
- **이력 탭**: 과거 운동 기록 리스트 + 지도

## 10. 우선순위 (작업 순서 권장)

1. 메트로놈 클릭음 실제 재생 구현 (현재 가장 시급한 누락 기능)
2. 무중단 타이머 + 거리 추적 로직 정리
3. 나이키 스타일 러닝 중 화면 UI 구성
4. TTS 음성 안내(1km 구간, 절반 지점, 목표 도달) + 성별 선택
5. SQLite 저장 및 이력 화면
6. GPS + 지도 경로 표시
7. 스트레칭 유튜브 연동
